.git
.github
.venv
tests
htmlcov
uv.lock
CHANGELOG.md
.python-version
.pre-commit-config.yaml

# Documentation and its build output. The image runs the tool; it does
# not serve the site.
docs
mkdocs.yml
site

# Not part of the runtime either. `action.yml` has to sit at the
# repository root -- that is the only place the Actions runner looks for
# it -- so it lands in the build context and has to be excluded by name.
examples
artifacts
action.yml

**/__pycache__
**/*.pyc
.mypy_cache
.pytest_cache
.ruff_cache
